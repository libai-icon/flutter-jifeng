import 'package:flutter/material.dart';

class LoinsColor {
  /// debug开关，上线需要关闭
  /// App运行在Release环境时，inProduction为true；当App运行在Debug和Profile环境时，inProduction为false
  static const Color themeColors = Color(0XFFFFFFFF);
  static const Color line = Color(0xFFEEEEEE);
  static const Color bottomNavigationBar = Colors.black87;
  static const Color loinsBgColor = Colors.white;
  static const Color text_gray = Color(0xFF9B9B9B);
}
